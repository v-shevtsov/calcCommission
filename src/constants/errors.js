import { CURRENCIES_SUPPORTED } from './types.js';

export const UNSUPPORTED_CURRENCY_ERROR = `Only supported currency: ${CURRENCIES_SUPPORTED}.`;

export const UNKNOWN_OPERATION_TYPE = 'Unknown operation';
