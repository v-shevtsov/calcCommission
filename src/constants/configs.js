import { API } from './API.js';

export const CONFIGS_URL = {
  cashInConfig: `${API}/cash-in`,
  cashOutJuridicalConfig: `${API}/cash-out-juridical`,
  cashOutNaturalConfig: `${API}/cash-out-natural`,
};
